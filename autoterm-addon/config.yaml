name: Autoterm Heater
version: "2.1.0"
slug: autoterm_heater
description: >-
  Controls an Autoterm-family diesel heater and its comfort panel over
  UART, publishing live status and exposing start/stop/thermostat controls
  as Home Assistant entities via MQTT discovery. Confirmed against a real
  Autoterm 5D / Flow 5 (BINAR-5S) unit -- see docs/PROTOCOL.md.
url: https://github.com/sinise/autoterm-heater-control
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
init: false
startup: services
boot: auto
uart: true
hassio_api: true
services:
  - mqtt:want
options:
  panel_port: /dev/ttyUSB1
  heater_port: /dev/ttyUSB3
  baud: 2400
  autodiscover_ports: false
  preheat_default_minutes: 30
  auto_target_default: 20.0
  prevent_freezing_target_default: 5.0
  mqtt_host: ""
  mqtt_port: 1883
  mqtt_username: ""
  mqtt_password: ""
schema:
  panel_port: str
  heater_port: str
  baud: int
  autodiscover_ports: bool
  preheat_default_minutes: int(1,600)
  auto_target_default: float(5,35)
  prevent_freezing_target_default: float(0,10)
  mqtt_host: str?
  mqtt_port: port?
  mqtt_username: str?
  mqtt_password: password?
